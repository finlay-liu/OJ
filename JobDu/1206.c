#include <stdio.h>
#include <stdlib.h>
#include <math.h>
// freopen("sample.txt", "r", stdin);
int main()
{
	int i;
	char s1[150], s2[150];
	while(scanf("%s%s", s1, s2) != EOF)
	{
		printf("%s%s\n", s1, s2);
	}
	return 0;
}